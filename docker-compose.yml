services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: orwell
      POSTGRES_PASSWORD: orwell
      POSTGRES_DB: orwell
    ports:
      - "5432:5432"
    volumes:
      - orwell_pgdata:/var/lib/postgresql/data
volumes:
  orwell_pgdata:
